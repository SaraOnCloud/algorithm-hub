# Global variables for docker-compose (HTTPS with Traefik)
# Copy as .env and adjust.

# Root domain (without protocol). E.g.: algorithmhub.dev or mydomain.com
DOMAIN=example.com

# Valid email for ACME registration (Let's Encrypt)
LE_EMAIL=admin@example.com

# (Optional) Use different values in staging vs production.
# For local testing without public DNS you can add entries to /etc/hosts pointing to 127.0.0.1
#   127.0.0.1 example.com
#   127.0.0.1 api.example.com
# NOTE: Let's Encrypt requires the domain to be publicly resolvable to issue real certificates.
