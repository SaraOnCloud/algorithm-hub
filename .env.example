# Variables globales para docker-compose (HTTPS con Traefik)
# Copiar como .env y ajustar.

# Dominio raíz (sin protocolo). Ej: algorithmhub.dev o midominio.com
DOMAIN=example.com

# Email válido para registro ACME (Let's Encrypt)
LE_EMAIL=admin@example.com

# (Opcional) Usar valores distintos en staging vs producción.
# Para pruebas locales sin DNS público puedes añadir entradas al /etc/hosts apuntando a 127.0.0.1
#   127.0.0.1 example.com
#   127.0.0.1 api.example.com
# NOTA: Let's Encrypt requiere que el dominio sea resoluble públicamente para emitir certificados reales.

